const baseURL = 'http://localhost:5000/api'

const urls = {
    items: '/items',
    users: {
        registration: '/users/signUp',
        logIn: '/users/logIn',
    },
}

export {
    baseURL,
    urls
}